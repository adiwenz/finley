# Handoff — issue 175

**Done so far:** Task 1 (down payment as an explicitly-funded obligation), Task 2 (reorder the month pipeline: explicit before automatic), Task 3 (gate's base follows the new order + gate == sim test), Task 4 (sibling explicit draws resolve in event sequence — coverage only, mechanism already present). Task 5 remains.

Note: #179's home-event decomposition has **not** landed on this branch — there is no `acquireAsset` primitive; the down payment is still emitted by the monolithic `HomePurchaseEvent` handler (`packages/engine/src/ledger/eventHandlers.ts` homePurchase.apply → `state.fundingDraws.push`). If #179 lands before task 5, move the obligation emission to `acquireAsset` as the issue directs.

## Live constraints
- **Resolution order is explicit → automatic** in `simulate.ts` (`simulateHousehold` month loop): `resolveFundingDraws` runs BEFORE `buildWithdrawalSources`. Do not re-invert. Explicit draws difference gains over `buildTaxableByOwner(nonWithdrawalSources)`; decumulation stacks on top.
- **Sibling explicit draws resolve in EVENT SEQUENCE.** `resolveFundingDraws` loops `state.fundingDraws` in ledger order (events applied by `sortedEvents` = month, then sequenceNumber) and drains balances/basis in place per draw, so each sibling sees what its predecessors left. There is NO sort inside the loop — the guarantee rides on push order. The seam snapshot `accountBalancesAfterFundingCents`/`accountBasisAfterFundingCents` (captured in `simulate.ts` right after `resolveFundingDraws`) is post-ALL-siblings, so the gate for an appended candidate is priced on what earlier same-month draws left. Pinned by `events.homePurchase.test.ts` describe `"sibling explicit draws resolve in event sequence"` (task 4).
- **Decumulation's gap is still sized on non-withdrawal income only** (`withdrawal.ts` `estimateNetIncome`). Explicit draws are net-neutral and must never move the gap.
- **The gate reads the pipeline seam "after explicit draws, before decumulation" on BOTH axes** — tax base (`flows.taxableByOwnerAfterFundingCents`) AND per-account balances/basis (`accountBalancesAfterFundingCents`/`accountBasisAfterFundingCents`), read in `ledger/addEvent.ts` `availabilityAt` via `balanceOf`/`basisOf`, falling back to end-of-month maps only for the `opening` snapshot (month ≤ 0, already pre-decumulation). See `simulate.types.ts` `ProjectionMonthFlows`.
- **gate == sim** (`resolveOrderedFundingDraw`, shared by `ledger/addEvent.ts` and the sim) is the load-bearing invariant. Locked by `events.homePurchase.test.ts` describes `"§4.5 gate == sim across a decumulation month"` and `"sibling explicit draws resolve in event sequence"`.
- The down-payment obligation is deliberately **not** in the list passed to `buildFlows`/`reportFlows`/`fundedLiabilityPayments`. Unchanged.

## Traps
- **A candidate's OWN untaxed gain does NOT seed the month's allocation tax base.** In `fundingDrawStep.ts` a draw's tax source is emitted only when `s.taxCents > 0`, so a candidate whose marginal gain sits under a threshold contributes its gain to `taxableByOwnerAfter` (pricing later draws/decumulation) but NOT to `allocateMonth`'s tax. To force a taxed decumulation month, make **decumulation's own** gain cross the threshold.
- **The gate reads the PRE-contribution, PRE-compounding balance** (the draw resolves at pipeline step 1, before `allocateMonth` contributions and `compoundAssets`). A money-out event for a fund still filling toward its target is priced on what has posted so far. `projectionFacade.test.ts` "names the events a goal's fund account pays for" had its down payment lowered $10k→$9k for exactly this.
- **No existing capital-gains figure moved through tasks 2–4.** No fixture combines a down-payment draw with decumulation on appreciated accounts in one month, so nothing shifted; the interaction is pinned only by the new tests. Task 4's sibling tests use `nullJurisdiction` to isolate the balance-drain axis, so they add no bracketed-jurisdiction figure either. If task 5 touches tax figures, build them fresh — do not expect an existing number to move.
- Shortfall in a decumulation month spills to the **synthetic credit card** (cascade), NOT to `isInsolvent`, whenever the card has room. Assert on `liabilityBalancesCents[SYNTHETIC_CARD_ID]` (+ one month interest — bound it, don't pin the cent).
- **Regression guard for report bands (task 5 will move this):** `events.homePurchase.test.ts` asserts exact `downpayment:brokerage` / `downpayment-tax:` band names and amounts (search `sourceId === "downpayment:brokerage"`). Task 5 re-keys bands off the obligation's `sourceId`+`treatment` and must update these assertions with band output value unchanged.
- The picker pool `sourcesAt` in `addEvent.ts` still reads end-of-month `accountBalancesCents` (display only, not the affordability verdict) — left as-is on purpose; `availabilityAt` is the gate.

## Deferred
- **Task 5** — Delete `FundingDraw`, `FundingReason` (its provenance-tag type), `REPORT_PREFIX` (in `fundingDrawStep.ts`), and the tax-blind ordered-drain helper `drainSources` (`ledger/funding.ts`) + its test `ledger/funding.test.ts`. Re-key report bands off the obligation's `sourceId`+`treatment` (band output unchanged). The tax-aware `resolveOrderedFundingDraw` and the fixed-amount account transfer both survive untouched. This is the last task: write `.sandcastle/summary-175.md` and delete this handoff in the same commit.

## Dead ends
- (none)
